/* import { BaseEntity } from 'typeorm';
import Board from '../resources/boards/boards.model';
import User from '../resources/users/user.model';

export class Service {
  table: Board | User;

  constructor(table: Board | User) {
    this.table = table;
  }

  public getAll = async (): Promise<void> => {
    const boards = await this.table.;
    // res.send(boards).log.debug(`Boards received from the base`);
  };
} */
